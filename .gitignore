# ═══════════════════════════════════════════════════
# ISLD Pipeline .gitignore
# ═══════════════════════════════════════════════════

# ── SQLite (GB級。絶対にコミットしない) ──
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# ── 実行生成物 ──
out*/
logs/

# ── 入力データ (GB級CSV) ──
ISLD-export/

# ── Python ──
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg
.eggs/
*.whl

# ── 仮想環境 ──
.venv/
venv/
env/

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── 環境変数 ──
.env
.env.local
.env.*.local

# ── example_ana 生成物 (再実行で再生成可能) ──
example_ana/out/
example_ana/outputs/

# ── sample_ntt 抽出結果 (再生成可能) ──
sample_ntt/*.csv

# ── 可視化生成物 (再生成可能) ──

# ── 一時スクリプト (デバッグ用) ──
_*.py

# ── null率レポート出力 ──
null_sample.csv
debug_raw_sample.csv
debug_unique_sample.csv

# ═══════════════════════════════════════════════════
# コミット対象
# ═══════════════════════════════════════════════════
# app/                      ← パイプライン本体
# config.json               ← メイン config
# requirements.txt          ← 依存パッケージ
# example_ana/              ← サンプル分析 (config + README)
# for_visual/               ← 可視化スクリプト
# sample_ntt/               ← NTT 固定ターゲット
# debug_flow.py             ← 自動検証
# debug_jobs.py             ← デバッグ用サンプル抽出
# null_rate_report.py       ← null率レポート
# README*.md                ← ドキュメント
# sample_visual.md          ← 可視化サンプル集
# IMPROVEMENTS.md           ← 改善提案
